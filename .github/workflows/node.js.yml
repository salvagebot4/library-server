name: Deploy to Heroku
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: akhileshns/heroku-deploy@v3.12.12
        with:
          branch: "main"
          heroku_api_key: ${{ secrets.HEROKU_API_KEY }}
          heroku_app_name: "library-management-server"
          heroku_email: "databasesalvageteam1@gmail.com"
